<Include xmlns="http://wixtoolset.org/schemas/v4/wxs">

  <!-- ════════════════════════════════════════════════════════════════════════════════════════════════
       What the bundle hands whichever MSI runs. Included into EVERY MsiPackage element.

       The packages cannot be factored into one - they have different SourceFiles and mutually exclusive
       InstallConditions, and Burn has no loop over a chain - but their contents can, and had to be. Written
       out per package, adding or renaming a setting is a three-site edit that nothing checks: exactly one of
       the packages ever runs on a given machine, so an omission from the x86 list ships as "that setting
       silently does nothing on 32-bit hosts", and CI runs the bundle only on an x64 runner - which cannot
       exercise the x86 or the arm64 list at all.

       The names live in Bundle.wxs (JasForwardedSettings and JasForwardedSecrets), which generates its
       Variable declarations from the same two lists - so the declarations and the forwarding cannot disagree.
       ════════════════════════════════════════════════════════════════════════════════════════════════ -->

  <!-- The architecture policy, in one line: whichever package matches the machine may replace an installed
       product of another architecture. The MSI's own gate stays strict for a bare .msi run by hand. -->
  <MsiProperty Name="FORCE_UPGRADE" Value="1" />

  <!-- Each setting is forwarded verbatim from the like-named bundle variable, and ONLY when that variable is
       non-empty. The Condition is load-bearing, not tidiness: an empty public property on the msiexec command
       line does not mean "unset", it means "set to empty", and it OVERRIDES the Property table default. This
       file previously passed every setting unconditionally, on the belief that an empty one would let the
       MSI's own default apply. It does the opposite. SERVICE_ACCOUNT= wiped 'NT SERVICE\Jenkins', so every
       bundle install registered the service with an empty StartName - LocalSystem - instead of the
       least-privilege virtual account the whole security posture is built on, and the data-folder ACL grant
       was aimed at an empty account name. util:PermissionEx accepted that silently; the EXE custom action
       that replaced it fails loudly, which is the only reason this was ever noticed.
       Nothing here restates an MSI default - that is still the point, and the Condition is what actually
       achieves it. -->
  <?foreach JasSetting in $(JasForwardedSettings) ?>
    <MsiProperty Name="$(JasSetting)" Value="[$(JasSetting)]" Condition="$(JasSetting) &lt;&gt; &quot;&quot;" />
  <?endforeach?>
  <?foreach JasSecret in $(JasForwardedSecrets) ?>
    <MsiProperty Name="$(JasSecret)" Value="[$(JasSecret)]" Condition="$(JasSecret) &lt;&gt; &quot;&quot;" />
  <?endforeach?>

</Include>
